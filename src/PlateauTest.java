import org.junit.Ignore;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by xuan on 11/11/15.
 */
/*
public class PlateauTest {
    @Test
    public void testGetCase() throws Exception {
        Joueur testJoueur = new Joueur("Paul");
        Plateau testPlateau = new Plateau(testJoueur);
        assertEquals(testPlateau.getCase(0).getContenu(), testJoueur);
        assertEquals(testPlateau.getCase(0).getNom(), "Depart");
    }

    @Test
    public void testIsFinPartie() throws Exception {
        Joueur testJoueur = new Joueur("Paul");
        Plateau testPlateau = new Plateau(testJoueur);
        assertEquals(testPlateau.isFinPartie(), false);
        testJoueur = testPlateau.getCase(0).getContenu();
        testPlateau.getCase(0).setContenu(null);
        testPlateau.getCase(1).setContenu(testJoueur);
        assertEquals(testPlateau.isFinPartie(), true);
    }

    @Test
    public void testTourDuJoueur() throws Exception {
        Joueur testJoueur = new Joueur("Paul");
        Plateau testPlateau = new Plateau(testJoueur);
        assertEquals(testPlateau.tourDuJoueur(), testJoueur);
        testJoueur = testPlateau.getCase(0).getContenu();
        testPlateau.getCase(0).setContenu(null);
        testPlateau.getCase(1).setContenu(testJoueur);
        assertEquals(testPlateau.tourDuJoueur(), testJoueur);
    }
    @Test(expected = Error.class)
    public void test2TourDuJoueur() {
        Plateau testPlateau = new Plateau(null);
        testPlateau.tourDuJoueur();
    }
}
*/